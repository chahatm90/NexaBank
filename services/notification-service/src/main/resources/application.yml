server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: notification-service
  jackson:
    default-property-inclusion: NON_NULL

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.banking: DEBUG
